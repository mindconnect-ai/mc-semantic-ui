{{!--
  UiCalendar → the whole element from CalendarPainter.java, the server-side
  twin of renderCalendar() in calendar/extension.ts. The painter, not the
  template, does the work, so the two stay one function apart.
--}}
{{{calendarHtml this}}}
