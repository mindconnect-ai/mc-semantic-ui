{{!--
  UiIcon → an <svg><use> into the curated sprite (or a literal emoji glyph).
  All the logic — token vs literal, sprite URL, accessible title — lives in
  IconRenderer via the {{{iconNode}}} helper, mirroring renderIconNode() in
  renderers/icon.ts.
--}}
{{{iconNode this}}}
