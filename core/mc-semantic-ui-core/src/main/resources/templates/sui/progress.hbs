{{!--
  UiProgress → a determinate/indeterminate bar or ring. All the arithmetic
  (percentage, SVG dash-offset) lives in ProgressRenderer via the {{{progress}}}
  helper, mirroring renderProgress() in renderers/progress.ts.
--}}
{{{progress this}}}
